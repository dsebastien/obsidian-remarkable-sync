# 2026-08-28: Catalog alignment + declarative settings port (fleet review)

## Accomplished

Three commits from the fleet-review pass (obsidian-plugin-template is the canonical source):

1. **Catalog reviewer alignment (A-E)**: export-ignores dropped for CHANGELOG.md/eslint.config.ts; changelog inlined via the `__PLUGIN_CHANGELOG__` build define (retiring this repo's own `.md`-loader workaround); eslint-plugin-obsidianmd 0.1.9 → 0.4.1 with `ui/sentence-case` as an error — brand `reMarkable` (deliberately NOT `reMarkable Cloud`: the copy writes "reMarkable cloud" and brands enforce casing both ways), JPEG/PNG/WebP brands, markdown → Markdown copy fixes, config-scoped exemptions for the desktop-only token import and the crypto utility (the 0.4.1 preset forbids inline disables). Type roots pinned; all 3 audit highs were overrides pinned AT vulnerable versions and were hand-corrected (fast-uri 3.1.5, brace-expansion 5.0.9, js-yaml 4.3.1 — `bun audit fix` cannot rewrite an override). Toolchain synced (bun 1.4.0, isolated tests, CI audit gate, unattended release.sh).

2. **Declarative settings port (feat!, minAppVersion 1.8.7 → 1.13.0)**: `getSettingDefinitions()` replaces the `display()` tab and its eight section components. The per-section `redisplay` callbacks are dead under this API; connect/disconnect, legacy-token removal and visibility-affecting writes call `update()` instead. `updateSettings` moved INSIDE the shared write queue with persist-then-commit on the merged data.json (settings + sibling token entries) — a settings save can no longer clobber a background token refresh. `setControlValue` validates (type, range, enum, URL); an invalid rmfakecloud URL is now refused with the framework's inline error (the old tab painted an error span but persisted anyway — deliberate behavior change).

3. **Adversarial-review fixes** (gpt-5.6-sol xhigh, after push on Sébastien's explicit go): production sourcemap back to `'none'` (the catalog reviewer's older Bun rejects the boolean form — this repo's own earlier lesson, clobbered by the template copy); `versions.json` boundary logic compares against the LATEST release's floor instead of file membership (this repo's floor went 1.13.0 → 1.8.7 → 1.13.0, so the membership check would have skipped the 2.0.0 → 1.13.0 boundary and offered the release to Obsidian versions lacking the declarative API); the connect/disconnect palette commands refresh the open settings pane; README requirement raised to Obsidian 1.13.0+.

## Verification

- 486 tests green (9 new settings-write tests with mutation-checked persist-then-commit ordering and write serialization, plus a token-preservation assertion; 4 new latestMinAppVersion tests covering the regressed-floor history), tsc clean, lint `--max-warnings 0` clean, build green, archive simulation green.
- Live vault check (real tokens in data.json): pane renders all groups; conditional rows hidden/shown correctly (the framework hides via `display:none` — verify with getComputedStyle, not DOM presence); auto-sync round-trip with the interval row appearing; out-of-range and invalid-URL writes rejected; tokens preserved through every write; probes reverted.

## Decisions

- Debounce on the interval slider and text inputs deliberately dropped: writes serialize through the queue and are small; revisit only if slow-storage backlog is observed in practice.
- End-to-end cloud sync remains untestable (device token revoked since session 2 — needs re-pairing with a one-time code).
