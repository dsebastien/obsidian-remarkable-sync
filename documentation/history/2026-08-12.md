# 2026-08-12

## Issue #19 — icons not loading on Android (investigation)

Report: on a Samsung tablet "the icons and buttons aren't loading text". No screenshot, no
versions, no indication of which surface. Investigated; **root cause not confirmed**.

Ruled out:

- **Icon names.** All names passed to `setIcon` (`import`, `refresh-cw-off`, `refresh-cw`,
  `tablet`, `folder`, `file`, `chevron-right`, `chevron-down`) exist at lucide `0.446.0`, the
  version Obsidian bundles. Checked each against the lucide repo at that tag.
- **Missing stylesheet.** Release 1.14.0 attaches `main.js`, `manifest.json`, `styles.css`.
- **Load failure from node builtins.** `dist/main.js` only reaches `require("node:fs"/"os"/"path")`
  inside the lazy `Platform.isDesktopApp` guard in the legacy token reader.
- **Missing image asset.** The Buy-me-a-coffee badge is inlined as a data URL.
- **Narrow-panel flex collapse.** Notebook rows already use `min-w-0` on the info column and
  `flex-shrink-0` on the checkbox, status dot and actions; the header is `flex-col`.
- **Hover-only controls.** No `:hover`-gated `opacity`/`visibility`/`display` anywhere.

Still open. Asked the reporter on the issue for a screenshot, which surface, Obsidian/Android/
Android System WebView versions, and a default-theme-no-other-plugins check. Also flagged that on
Android plugin ribbon icons live in the **right** sidebar — if they were hunting for the panel
rather than looking at a broken one, that is the whole answer.

The WebView version matters: below Chromium 99 there is no `@layer` support, which drops the
entire Tailwind output including the theme variables, leaving every `calc(var(--spacing) * n)`
invalid.

## Dropped Tailwind Preflight from the shipped stylesheet

Found while investigating the above, fixed on its own merits rather than as a confirmed fix for
#19.

`src/styles.src.css` opened with `@import 'tailwindcss'`, which pulls in Preflight. A plugin
stylesheet is injected into the whole Obsidian document, so that reset applied app-wide, not just
to this plugin's views: `* { margin: 0; padding: 0; border: 0 solid }`, `svg { display: block }`,
`ol, ul { list-style: none }`, `button { background: transparent; color: inherit }`.

Preflight lands in `@layer base`, and unlayered rules beat layered ones regardless of specificity,
so Obsidian's own CSS neutralised most of it on desktop — which is why nobody noticed. It still
won on every element Obsidian leaves at user-agent defaults, and the mobile app's chrome is a
different DOM with more of that surface exposed.

Replaced with theme + utilities only:

```css
@import 'tailwindcss/theme' layer(theme);
@import 'tailwindcss/utilities' layer(utilities);
```

Output drops from 22.2 KB to 19.0 KB and no longer contains `@layer base`.

### Compensating for what Preflight provided

Removing a global reset means anything that silently depended on it has to be pinned. Three
places did:

- **`box-sizing: border-box`.** Restored, scoped to `[class^='remarkable-']` and its descendants.
  Deliberately _not_ a blanket descendant reset: these rules are unlayered, so a
  `* { margin: 0 }` here would outrank Obsidian's own `.markdown-rendered` spacing inside the
  "What's new" release notes and wreck them.
- **Unclassed `<p>` in the panel's empty and disconnected states.** Now explicitly `m-0`, matching
  what Preflight gave them.
- **Two unclassed headings** that were rendering at inherited size and normal weight purely
  because Preflight neutered them — the import-confirm modal's `h3` and the "What's new" `h2`.
  Both now carry a class and explicit metrics instead of depending on user-agent defaults.

The two headings are an **intentional visual change**: they will now look like headings rather
than body text. That was the evident design intent; Preflight was hiding it.

### Needs manual verification in a real vault

Nothing here is verifiable from a build. Check:

- reMarkable panel: header buttons, folder rows, notebook rows, progress bars.
- Connect-to-cloud modal, import-confirm modal (title now semibold), settings tab.
- "What's new" tab: title size, and that the rendered release-notes markdown still has normal
  paragraph and list spacing (this is the one the scoped reset was written to protect).
- Ideally on Android, to see whether it moves #19 at all.
