# 2026-08-01

## Panel sorting (branch `feat/panel-sorting`, off `main`)

The panel listed notebooks in whatever order the cloud returned them, which for 50 daily journals
is effectively random. Folders were already sorted; notebooks within them were not.

Added `panelSortOrder` (default `modified-desc`) with four orderings, driven by a pure
`sortNotebooks` in `domain/notebook-sort.ts` with 15 specs. Sorting happens in
`getFilteredNotebooks`, so the cloud listing is kept as received and changing the order costs only
a re-render; the grouping downstream preserves it per folder.

Two details worth keeping:

- Name comparison uses `localeCompare` with `numeric: true`. A plain string sort puts
  "Notebook 10" before "Notebook 2", which reads as broken.
- Date sorting breaks ties on name. Without a total order, notebooks sharing a timestamp can swap
  places between renders, which looks like the list flickering.

Branched from `main` rather than stacking on `feat/pdf-export`: unrelated feature, and it keeps
both reviewable on their own.

**Note for whichever branch merges second:** this branch had to add `panelSortOrder` to the
hand-written allow-list in `loadSettings`, because the structural fix that makes that impossible to
forget lives on `feat/pdf-export`. Whoever merges second should drop the hand-written line in
favour of the iterating version.


## PDF export plan written

Plan: `documentation/plans/pdf-export.md`. No code yet.

Scoping: "PDF support" was ambiguous across three separate features. User chose **notebook to PDF
export** (an output-format option next to PNG/JPEG/WebP). The other two, syncing PDF-backed
documents and annotation burn-in, are recorded as out of scope in that plan.

Audited the pipeline to answer two questions the user asked directly:

- **Is there PDF export today?** No. `renderPage()` encodes to PNG/JPEG/WebP only and
  `writePageImage()` writes one file per page.
- **Is there annotation-over-PDF today?** No. `downloadDocument()` does fetch the `<id>.pdf` blob
  (it downloads every entry in the document index), but `parseDocument()` only reads `.metadata`,
  `.content` and `.rm`, so the PDF is dropped. A PDF-backed document syncs as ink on blank white
  pages, or reports "No pages with content found" if it was never annotated.

Design calls made in the plan, both wanting confirmation before implementation starts:

- **No new dependency.** Hand-roll a minimal PDF 1.4 writer instead of pulling `pdf-lib` (~380 KB
  minified against a ~129 KB bundle, most of it standard-font data this feature never uses). An
  image-only PDF needs JPEG passthrough as `/DCTDecode` and a zlib stream for the lossless path,
  and `fflate` (already bundled, already blessed by the `fflate/browser` rule) supplies the latter.
  Nothing new for the catalog reviewer to flag.
- **New `outputFormat` setting** rather than adding `'pdf'` to `imageFormat`, which drives both the
  canvas encoder and the file extension. `saveImages` folds into it as `'none'` via a load-time
  migration, otherwise the settings tab carries two overlapping switches.

Open decisions listed for the user: whether to fold `saveImages`, and how a PDF should represent
blank and failed-to-render pages, since dropping them silently shifts every later page number.
